<idea-plugin>
    <id>branchmanager</id>
    <name>Git Branch Manager</name>
    <vendor email="mirian.dzhachvadze@gmail.com">Mirian Dzhachvadze</vendor>
    <version>1.0.0</version>
    <description><![CDATA[
        Plugin provide actions like checking out same branch in multiple Git repositories
        and remove unneeded local branches. It's very useful in projects with several maven
        or gradle modules loaded in the IDE.
        Plugin has a GUI interface that allow to select repositories and branch to be created.
        Also it's possible to switch repositories to already existed branch. Plugin find common branches
        in selected repositories and suggest to choose one to be checked out.
        Another feature is to remove unneeded branches. For this case plugin also provide GUI with
        list of loaded repositories. It shows branches that do not have tracking remotes and
        allow to delete them.
        Plugin available at VCS -> Branch Manager
    ]]></description>

    <!-- please see http://www.jetbrains.org/intellij/sdk/docs/basics/getting_started/plugin_compatibility.html
         on how to target different products -->
     <!--enable plugin in all products(such as IntelliJ IDEA, RubyMine, WebStorm)-->
    <depends>com.intellij.modules.lang</depends>
    <depends>Git4Idea</depends>

    <change-notes><![CDATA[
        It's the first plugin version. No change notes since last release.
    ]]></change-notes>

    <actions>
        <group id="app.BranchManagerGroup"
               class="app.BranchManagerGroup"
               text="Branch manager" popup="true">
            <add-to-group group-id="VcsGroups" anchor="last"/>
            <action id="app.CreateBranchAction"
                    class="app.CreateBranchAction"
                    text="Checkout branch"
                    description="Create same branch in selected repositories"
            />
            <action id="app.PurgeLocalBranchesAction"
                    class="app.PurgeLocalBranchesAction"
                    text="Purge local branches"
                    description="Delete local branches missing in remote"
            />
        </group>
    </actions>

</idea-plugin>